# For local installs the course environment already has all of this.
# On Hugging Face Spaces, gradio itself comes from the Space's sdk_version,
# so it is deliberately NOT listed here.
langchain>=1.3
langchain-google-genai>=4.0
langgraph>=1.0
python-dotenv>=1.0

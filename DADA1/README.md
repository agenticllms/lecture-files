# DADA1 — Trip Planner Agent

**Fill this in before you submit.** Keep the line exactly in this form - it is
how your deployed app gets found and graded.

```
Deployed app: <your Render URL>
```

Read `DADA1_SPEC.md` first.

Get the files the same way as every class day: pull, then work in a copy:

```bash
cd lecture-files
git pull
cd ..
cp -r lecture-files/DADA1 DADA1     # work in the copy, not in lecture-files/
cd DADA1
```

```bash
cd trip_planner
pip install -r requirements.txt
python -m agent.agent        # chat in the terminal
python app.py                # web UI
```

